version: "3"
services:
  xui:
    build: 
      context: .
      dockerfile: dockerfile
    privileged: true
    volumes:
      - xui_data:/home/xui
      - mysql_data:/var/lib/mysql
    restart: unless-stopped

volumes:
  xui_data:
  mysql_data:
